version: '2'
services:
        master:
                container_name: autobuilder-master
                image: b1gtuna/yocto-builder:latest
                environment:
                        - TZ="America/New_York"
                hostname: TARS
                #volumes:
                #        - /var/build:/home/builder/build
